A 股量化分析工具 — v0.1.0

发布日期: 2026-08-05

概述
---
首个公开版本（v0.1.0），这是一个轻量的单文件量化分析脚本，面向 A 股日线数据的下载、技术指标计算、双均线策略回测与参数扫描，并提供可视化与 CSV 导出。

主要亮点
---
- 支持腾讯（优先）/新浪（回退）两路数据源，带自动重试与 User-Agent，增强下载稳定性
- 计算常用技术指标：MA5 / MA20 / RSI(14)
- 双均线策略回测（信号次日生效）与参数扫描（按夏普比率排序）
- 样本外验证：前 70% 选参，后 30% 检验，判断过拟合风险
- 可视化：综合看板与最优策略图（带买卖点标注）
- 在无 GUI 环境自动回退到 Agg 后端，仍能输出报告与 CSV

修复与改进（重要事项）
---
- 修复：修正“20 日累计涨幅”计算，改为与 20 个交易日前收盘价比较（修复了误用昨日收盘价导致的错误）
- 改进：图表后端自动回退逻辑（TkAgg → Agg），并在无 GUI 时给出友好提示
- 修复：当回退到新浪数据源时，统一清理市场前缀（如 sh600519 → 600519）以便展示一致性
- 修复：参数扫描中剔除样本过少导致的 NaN 夏普值，避免排序时 NaN 排在最前
- 改进：下载失败时的友好错误处理（两个数据源都失败时返回可读错误信息，不再抛出长 traceback）

安装与运行（快速）
---
1. 在虚拟环境中安装依赖：
   pip install pandas requests matplotlib
2. 运行：
   python a_share_quant.py 600519

兼容性与注意事项
---
- 推荐 Python 3.10 及以上
- 在受管或特殊发行版的 Python（如 MSYS2）中可能需要额外安装包管理工具或使用系统包管理器
- 若无网络，可在有网络的机器运行一次脚本以生成 <ticker>.csv，再把该 CSV 拷贝到目标机器离线使用（后续可选加入本地优先加载功能）

发布附件建议
---
- 源代码（整个仓库）
- 示例输出 CSV（可选，注意不要包含敏感信息）
- 一两张截图（综合看板 / 最优策略图），放在 assets/ 目录便于 README 展示

发布说明（供粘贴到 GitHub Releases 的正文）
---
本次发布为 v0.1.0 的首个公开版本：
- 首发：股票日线下载、指标计算、双均线回测与参数扫描、样本外验证与可视化。
- 修复若干数据与展示相关的 bug（详见 CHANGELOG）。

感谢
---
感谢所有测试与反馈的朋友。如遇问题或希望新增功能，请在 Issues 中提出。